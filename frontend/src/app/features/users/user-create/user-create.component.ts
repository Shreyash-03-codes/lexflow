import { Component } from '@angular/core';

@Component({
  selector: 'app-user-create',
  standalone: true,
  template: `<p>Create user (inline modal used in list view)</p>`,
})
export class UserCreateComponent {}
