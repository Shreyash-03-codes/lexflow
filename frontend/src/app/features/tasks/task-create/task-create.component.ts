import { Component } from '@angular/core';

@Component({
  selector: 'app-task-create',
  standalone: true,
  template: `<p>Create task (inline modal used in task list view)</p>`,
})
export class TaskCreateComponent {}
