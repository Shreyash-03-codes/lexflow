import { Component } from '@angular/core';

@Component({
  selector: 'app-case-create',
  standalone: true,
  template: `<p>Create case (inline modal used in list view)</p>`,
})
export class CaseCreateComponent {}
