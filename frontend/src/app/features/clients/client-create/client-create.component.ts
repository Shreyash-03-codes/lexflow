import { Component } from '@angular/core';

@Component({
  selector: 'app-client-create',
  standalone: true,
  template: `<p>Create client (inline modal used in list view)</p>`,
})
export class ClientCreateComponent {}
